import React from 'react';
import { RenderCards, Slide } from '../components';

const Home = () => {
    return (
        <div>
            Home
            <Slide />
            <RenderCards />
        </div>
    );
};

export default Home;
